
	Jednym z podej¶æ do wykrywania ruchu na obrazie z kamery jest wykorzystanie tzw. ,,Optical Flow" (\textit{,,przep³yw optyczny"}) \cite{flow}, czyli wzorca ruchu, który dla obrazów 2D jest wzglêdn± prêdko¶ci± wyznaczon± dla wybranych pikseli na dwóch zdjêciach, a w przypadku tworzonej aplikacji, dla dwóch s±siednich klatek nagrania. Wydaje siê to najbardziej naturalny sposób podej¶cia do problemu. Optical Flow jest okre¶lony poprzez dwa podstawowe za³o¿enia:
	\begin{enumerate}
	\item Jasno¶æ pikseli nie zmienia siê pomiêdzy dwoma klatkami,
	\item S±siednie piksele s± przesuniête o t± sam± odleg³o¶æ ($\Delta x$,$\Delta y$):
	\begin{equation}
 	I(x,y,t) = I(x+\Delta x,y+\Delta y,t+\Delta t).
	\end{equation}
	\end{enumerate}
	Zak³adaj±c, ¿e to przesuniêcie jest ma³e, praw± stronê równania rozwija siê w szereg Taylora i otrzymuje:
	\begin{equation}
 	I(x+\Delta x,y+\Delta y,t+\Delta t)= I(x,y,t) + \frac{ \partial I}{ \partial x} \Delta x + \frac{ \partial I}{ \partial y} \Delta y + \frac{ \partial I}{ \partial t} \Delta t +o(\Delta^2), 
	\end{equation}
	gdzie $o(\Delta^2)$ to czynniki coraz wy¿szych rzêdów, które mog± zostaæ pominiête. 
	Z tego otrzymuje siê nastêpuj±ce równania:
	\begin{equation}
    \frac{ \partial I}{ \partial x} \Delta x + \frac{ \partial I}{ \partial y} \Delta y + \frac{ \partial I}{ \partial t} \Delta t =0,
	\end{equation}
	lub 
	\begin{equation}
    \frac{ \partial I}{ \partial x} \frac{\Delta x}{\Delta t} + \frac{ \partial I}{ \partial y} \frac{\Delta y}{\Delta t} + \frac{ \partial I}{ \partial t} \frac{\Delta t}{\Delta t} =0,
	\end{equation}
	co w rezultacie daje:
	\begin{equation}
    \frac{ \partial I}{ \partial x} V_x + \frac{ \partial I}{ \partial y} V_y + \frac{ \partial I}{ \partial t} =0,
	\end{equation} 
	gdzie $V_x$,$V_y$ to sk³adowe $x$ i $y$ prêdko¶ci albo inaczej przep³yw optyczny $I(x,y,t)$, natomiast $\frac{ \partial I}{ \partial x}$,$\frac{ \partial I}{ \partial y}$ i $\frac{ \partial I}{ \partial t}$ to pochodne cz±stkowe obrazu  w $(x,y,t)$. Inaczej mo¿na to przedstawiæ jako:
	\begin{equation}
	I_x V_x + I_y V_y = - I_t.
	\end{equation}
	
	
	W równaniu tym s± jednak dwie niewiadome ($V_x$ i $V_y$), co uniemo¿liwia jego rozwi±zanie w sposób dok³adny i jednoznaczny. Jedn± z metod, która przybli¿a jego rozwi±zanie, to algorytm Lucasa-Kanade \cite{luc}.  Metoda ta opiera siê na za³o¿eniu, ¿e przesuniêcie zawarto¶ci obrazu miêdzy dwoma ramkami jest minimalne i zak³ada, ¿e s±siedztwo piksela (9 znajduj±cych siê dooko³a pikseli) ma podobnych ruch. Wobec tego jest rozwi±zywane 9. równañ z dwiema zmiennymi, czyli uk³ad jest nadokre¶lony:
	\begin{eqnarray*}
\begin{bmatrix}
V_x   \\ 
V_y   
\end{bmatrix} = 
\begin{bmatrix}
\sum_{i=1} I_x (q_i)^2 & \sum_{i=1} I_x (q_i) I_y (q_i) \\ 
\sum_{i=1} I_y (q_i) I_x (q_i) & \sum_{i=1} I_y (q_i)^2 
\end{bmatrix}^{-1}
\begin{bmatrix}
\sum_{i=1} I_x (q_i) I_t (q_i) \\ 
\sum_{i=1} I_y (q_i) I_t (q_i)
\end{bmatrix},
\end{eqnarray*}
gdzie i=1 do 9. Algorytm estymuje rozwi±zanie poprzez wykorzystanie metody najmniejszych kwadratów \cite{bjork}.
